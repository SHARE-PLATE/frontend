import styled from 'styled-components';

export const categoryButtonHeight = '3.6rem';

export const Wrapper = styled.ul`
  display: flex;
  gap: 0.75rem;
  align-items: center;
  height: ${categoryButtonHeight};
`;

export const Item = styled.li``;
